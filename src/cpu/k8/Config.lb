# How does the config tool pick the correct
# cpufixup.c to generate cupfixup.o ?

uses k8
uses CPU_FIXUP
default k8=1
dir /cpu/k7
config chip.h
if CPU_FIXUP
  object cpufixup.o
  object apic_timer.o
end
